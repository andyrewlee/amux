// Package sidebar implements the sidebar pane: the Changes tab (branch-change
// list with diffs and workspace actions), the Project tab (workspace file
// tree), and an embedded tmux-backed terminal with its own PTY I/O and text
// selection.
package sidebar
